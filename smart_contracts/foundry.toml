[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options

[etherscan]
opBNB_testnet = { key = "${OP_BNB_TESTNET_API_KEY}", chain = "5611", url = "${OP_BNB_TESTNET_API_URL}" }
linea_sepolia = { key = "${LINEA_SEPOLIA_API_KEY}", chain = "59141", url = "${LINEA_SEPOLIA_API_URL}" }
neon_evm_devnet = { key = "${NEON_EVM_DEVNET_API_KEY}", chain = "245022926", url = "${NEON_EVM_DEVNET_API_URL}" }
